'use client';

import { useCallback, useEffect, useState } from 'react';
import Link from 'next/link';

type Product = { id: string; title: string; slug: string; status: string; basePricePoisha: number; productPoint: number; sku?: string | null; category?: { name?: string } | null; brand?: { name?: string } | null; version: number };

export default function AdminProductsPage() {
  const [products, setProducts] = useState<Product[]>([]);
  const [status, setStatus] = useState('');
  const [search, setSearch] = useState('');
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const load = useCallback(async () => { try { setLoading(true); const params = new URLSearchParams(); if (status) params.set('status', status); if (search) params.set('search', search); const response = await fetch(`/api/v1/admin/catalog/products?${params}`); const json = await response.json().catch(() => null); if (!response.ok || !json?.success) throw new Error(json?.error?.message || 'Unable to load products.'); setProducts(json.data || []); setError(null); } catch (err: any) { setError(err.message || 'Unable to load products.'); } finally { setLoading(false); } }, [status, search]);
  useEffect(() => { void load(); }, [load]);
  return <main className="min-h-screen bg-slate-50 p-4 text-slate-900 sm:p-8"><div className="mx-auto max-w-7xl space-y-6"><header className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-widest text-indigo-600">Catalog Administration</p><h1 className="text-3xl font-black">Products</h1><p className="mt-1 text-sm text-slate-600">Review catalog records, approval state, pricing, points, and taxonomy context.</p></div><div className="flex gap-2"><Link href="/admin/moderation" className="rounded-lg border border-indigo-200 bg-indigo-50 px-4 py-2 text-sm font-bold text-indigo-700">Moderation</Link><Link href="/admin/categories" className="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-semibold">Categories</Link></div></header>{error && <div className="rounded-xl bg-rose-50 p-4 text-sm text-rose-700">{error}<button type="button" onClick={() => void load()} className="ml-3 font-bold underline">Retry</button></div>}<div className="flex flex-wrap gap-3"><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search title or SKU" className="rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm" /><select value={status} onChange={(event) => setStatus(event.target.value)} className="rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm"><option value="">All statuses</option><option value="DRAFT">Draft</option><option value="PENDING_APPROVAL">Pending approval</option><option value="APPROVED">Approved</option><option value="PUBLISHED">Published</option><option value="ARCHIVED">Archived</option></select></div>{loading ? <div className="rounded-xl border border-slate-200 bg-white p-6 text-sm text-slate-500">Loading products…</div> : products.length === 0 ? <div className="rounded-xl border border-dashed border-slate-300 bg-white p-8 text-center text-sm text-slate-500">No products match the selected filters.</div> : <div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">Product</th><th className="px-4 py-3">Taxonomy</th><th className="px-4 py-3">Price</th><th className="px-4 py-3">Points</th><th className="px-4 py-3">Status</th></tr></thead><tbody>{products.map((product) => <tr key={product.id} className="border-t border-slate-100"><td className="px-4 py-4"><div className="font-bold">{product.title}</div><div className="font-mono text-xs text-slate-500">{product.sku || product.slug}</div></td><td className="px-4 py-4 text-slate-600">{product.category?.name || '—'}<br />{product.brand?.name || 'No brand'}</td><td className="px-4 py-4 font-mono">৳{(Number(product.basePricePoisha) / 100).toLocaleString('en-BD', { minimumFractionDigits: 2 })}</td><td className="px-4 py-4">{product.productPoint}</td><td className="px-4 py-4"><span className="rounded-full bg-slate-100 px-2 py-1 text-xs font-bold">{product.status}</span></td><td className="px-4 py-4"><Link href={`/admin/products/${product.id}/versions`} className="font-semibold text-indigo-700 hover:underline">History</Link></td></tr>)}</tbody></table></div>}</div></main>;
}
